#include <bits/stdc++.h>
#define endl "\n"
#define ll long long
using namespace std;

signed main()
{
	ios::sync_with_stdio(false);
	cin.tie(nullptr), cout.tie(nullptr);

	string n;
	cin >> n;
	//reverse(n.begin(), n.end());
	for(auto c:n)
		cout << c << endl;
	
	return 0;
}                