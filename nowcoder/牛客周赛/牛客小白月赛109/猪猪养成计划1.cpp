#include <bits/stdc++.h>
#define endl "\n"
#define ll long long
using namespace std;

signed main()
{
	ios::sync_with_stdio(false);
	cin.tie(nullptr),cout.tie(nullptr);

	int n, k1, k2;
	cin >> n >> k1 >> k2;
	vector<int> a(n);
	for (int i = 0; i < n;i++)
	{
		cin >> a[i];
	}

		return 0;
}