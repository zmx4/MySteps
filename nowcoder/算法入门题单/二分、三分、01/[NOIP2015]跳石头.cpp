#include <bits/stdc++.h>
#define endl "\n"
#define ll long long
using namespace std;
const ll MAXNUM = 1e6+10;
int a[MAXNUM];



signed main()
{
	ios::sync_with_stdio(false);
	cin.tie(nullptr);cout.tie(nullptr);

	ll L, N, M;
	cin >> L >> N >> M;
	for (int i = 1; i <= N; i++)
	{
		cin >> a[i];
	}

	return 0;
}