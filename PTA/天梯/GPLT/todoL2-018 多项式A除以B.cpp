#include <bits/stdc++.h>
#define endl "\n"
#define ll long long
using namespace std;

signed main()
{
	ios::sync_with_stdio(false);
	cin.tie(nullptr),cout.tie(nullptr);

	int n;
	cin >> n;
	vector<pair<int, int>> fs;
	for (int i = 0; i < n;i++)
		cin >> fs[i].first;
	for (int i = 0; i < n;i++)
		cin >> fs[i].second;
	for (int i = 0; i < n; i += 2)
	{
		pair<int, int> a, b;
	}
		return 0;
}