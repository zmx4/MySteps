#include <iostream>
#include <string>
using namespace std;

int _Num1(long a,long b)
{
    return a*b;
}

int _Num2(string a,string b)
{
    int lengthA=a.length();
    int lengthB=b.length();
    int A[lengthA];
}