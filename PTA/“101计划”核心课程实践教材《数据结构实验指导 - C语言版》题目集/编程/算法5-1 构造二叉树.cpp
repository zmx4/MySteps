#include <bits/stdc++.h>
#define endl "\n"
#define ll long long
using namespace std;

signed main()
{
	ios::sync_with_stdio(false);
	cin.tie(nullptr),cout.tie(nullptr);

	int a, b, c;
	cin >> a >> b >> c;
	cout << "root data = " << c << endl;
	cout << "left child data = " << a << endl;
	cout << "right child data = " << b << endl;
		return 0;
}