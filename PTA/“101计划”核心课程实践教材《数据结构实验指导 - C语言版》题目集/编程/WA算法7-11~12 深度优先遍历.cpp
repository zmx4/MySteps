#include <bits/stdc++.h>
#define endl "\n"
#define ll long long
using namespace std;

signed main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr),cout.tie(nullptr);

    int kMaxVertex;
    cin >> kMaxVertex;
    int n,m;
    cin >> n >> m;
    vector<char> vertex(n);
    for (int i = 0; i < n; i++)
        cin >> vertex[i];

    return 0;
}